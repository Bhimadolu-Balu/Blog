import React from 'react'
import {AppBar, Toolbar} from '@mui/material'
const Header = () => {
  return (
   <AppBar>
    <Toolbar>
        Balu varma
    </Toolbar>
   </AppBar>
  )
}

export default Header